class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int pre = 0, ret = nums[0];
        for(int &x: nums){
            pre = max(pre + x, x);
            ret = max(pre, ret);
        }
        return ret;
    }
};
